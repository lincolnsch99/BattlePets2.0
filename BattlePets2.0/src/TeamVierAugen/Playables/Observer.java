/**
 * This is a interface for observers.
 */
package TeamVierAugen.Playables;

public interface Observer {
    /**
     * methods to update event of every player.
     */
    public void update(Object event);
}
